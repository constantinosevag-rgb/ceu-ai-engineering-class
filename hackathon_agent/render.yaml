services:
  - type: web
    name: hackathon-agent
    env: python
    plan: free
    buildCommand: "pip install -r hackathon_agent/requirements.txt"
    startCommand: "chainlit run hackathon_agent/agent.py --host 0.0.0.0 --port 10000"
    envVars:
      - key: PORT
        value: 10000
